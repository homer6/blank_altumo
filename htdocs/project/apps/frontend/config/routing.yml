# You can find more information about this file on the symfony website:
# http://www.symfony-project.org/reference/1_4/en/10-Routing

# default rules
homepage:
  url:   /
  param: { module: public, action: index }

# sample api route
api_list_users:
  url:   /api/user/list
  param: { module: public, action: index }
  options:
    export_to_javascript: true